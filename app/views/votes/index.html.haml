%h1 Listing votes

%table
  %tr
    %th Id
    %th Votable
    %th Votable type
    %th Voter
    %th Voter type
    %th Vote flag
    %th Created at
    %th Updated at
    %th
    %th
    %th

  - @votes.each do |vote|
    %tr
      %td= vote.id
      %td= vote.votable_id
      %td= vote.votable_type
      %td= vote.voter_id
      %td= vote.voter_type
      %td= vote.vote_flag
      %td= vote.created_at
      %td= vote.updated_at
      %td= link_to 'Show', vote
      %td= link_to 'Edit', edit_vote_path(vote)
      %td= link_to 'Destroy', vote, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Vote', new_vote_path
